<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.SystemPromptMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into system_prompt (name, system_prompt, avatar, description, user_id, create_time)
        values (#{name}, #{systemPrompt}, #{avatar}, #{description}, #{userId}, #{createTime})
    </insert>

    <update id="update">
        update system_prompt
        set name = #{name},
            system_prompt = #{systemPrompt},
            avatar = #{avatar},
            description = #{description}
        where id = #{id}
    </update>

    <delete id="delete">
        delete from system_prompt where id = #{id}
    </delete>

    <select id="getById" resultType="SystemPrompt">
        select * from system_prompt where id = #{id}
    </select>

    <select id="getByUserId" resultType="SystemPrompt">
        select * from system_prompt
        where user_id = 0 or user_id = #{userId}
        order by user_id asc, create_time desc
    </select>

    <select id="getSystemBuiltIn" resultType="SystemPrompt">
        select * from system_prompt where user_id = 0 order by create_time asc
    </select>

</mapper>
